- name: demo on loop
  hosts: local
  connection: local
  tasks: 
  - name: print names
    ansible.builtin.debug:
      msg: "hi {{ item }}" #item is reserved keywords
    loop:
    - sreeja
    - aishwarya
    - renuka